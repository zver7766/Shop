@model Car

<div class="col-lg-4">
    <img class="img-thumbnail" width="400" src="@Model.Img" alt="@Model.Name"/>
    <h2>@Model.Name</h2>
    <p>@Model.ShortDesc</p>
    <p>Цена : @Model.Price.ToString("c")</p>
    <p><a class="btn btn-warning" href="#">Подробнее</a></p>
</div>